# Stage 1: Build the application using Maven
# Use a specific version of Maven with a modern JDK (Java 17 here)
FROM maven:3.8.5-openjdk-17 AS build

# Set the working directory inside the container
WORKDIR /app

# Copy the Maven project file to download dependencies first (for better caching)
COPY pom.xml .

# Download all dependencies
RUN mvn dependency:go-offline

# Copy the rest of your application's source code
COPY src ./src

# Package the application, skipping tests for faster builds in deployment
RUN mvn package -DskipTests


# Stage 2: Create the final, lightweight runtime image
# Use an official JRE (Java Runtime Environment) which is smaller than a full JDK
FROM openjdk:17-jre-slim

# Set the working directory
WORKDIR /app

# Copy the built .jar file from the 'build' stage
COPY --from=build /app/target/*.jar app.jar

# Expose the port that Spring Boot runs on (8080 is the default)
EXPOSE 8080

# The command to run your application when the container starts
ENTRYPOINT ["java","-jar","app.jar"]